# kakidas v0.5.9 — 振り番の表示・出力

## 追加

- メモ編集画面に「番号を表示」スイッチを追加。初期状態はオフ。
- 単語・文・段落の全項目に、現在の並び順に基づく振り番を表示できる。
- 階層化した単語・文は `1` / `1.1` / `1.1.1` のように、階層を反映して番号を再計算する。
- 番号を表示中は、全体コピー・種別ごとのコピー・.txt出力・個別コピーにも同じ番号を含める。
- 並び替え、右へ下げる、左へ戻す後も、番号は保存せずその場で再計算する。

## 変更しないもの

- entries テーブル、kind、parent_id、sort_order、クラウド同期の構造は変更なし。
- 番号は画面表示・コピー用の派生情報で、IndexedDBやSupabaseへ新しい列は追加しない。
- 番号表示の設定は端末・ブラウザごとのlocalStorageに保存する。
