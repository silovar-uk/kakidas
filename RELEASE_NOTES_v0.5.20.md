# kakidas v0.5.20 — クラウド削除とモバイル備考＋

## 変更

- クラウドのメモ一覧から、各メモを**クラウドだけから削除**できるように追加
  - 削除前に確認を表示
  - この端末のローカルメモ・項目は残る
  - 削除後、同じローカルメモは「ローカルのみ」状態へ戻り、必要なら改めて送信できる
  - クラウド側の関連項目はメモと同時に削除される
- スマホでは、本文下の `＋ 気持ち・備考` を非表示に変更
- スマホの右側操作レールへ、コンパクトな `＋` ボタンを追加
  - `＋` から備考を追加・編集できる
  - 備考本文がある場合は、これまでどおり本文下にだけ表示
- PCでは、従来どおり本文下の `＋ 気持ち・備考` を常設

## Supabase

既存の `supabase/schema.sql` にある `memos` / `entries` のDELETEポリシーと
`ON DELETE CASCADE` を利用します。追加のSQL実行は不要です。
