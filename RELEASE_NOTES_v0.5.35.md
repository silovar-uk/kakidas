# kakidas v0.5.35 — モバイルのメモ削除確認

## 修正

- メモ一覧の `×` による削除確認を、ブラウザ標準の `window.confirm()` からアプリ内の確認画面へ変更しました。
- iPhone Safariで標準確認が表示・復帰しない場合でも、`×` を押すと削除確認を開き、そこで `削除する` を選ぶとローカルメモを削除できます。
- `×` の大きさ・置き場所・メモ一覧のレイアウトは変更していません。
- 削除は従来どおりローカルのメモと項目にだけ反映され、クラウドへ送ったコピーは残ります。

## データ・同期

- IndexedDBの構造、Supabase、クラウド同期、既存メモ、バックアップ形式に変更はありません。
- Supabase SQLの追加実行は不要です。
