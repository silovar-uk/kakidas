# kakidas v0.5.37 — 一時通知の自動消去

## 変更

- 「完了済み3件を削除しました。」「コピーしました。」「バックアップを書き出しました。」などの完了通知を、表示から5秒後に自動で閉じるようにしました。
- 通知の右側に `×` を追加し、待たずに手動で閉じられるようにしました。
- メモ一覧、メモ編集画面、クラウドライブラリで同じ挙動に統一しました。
- エラー表示や削除確認ダイアログは、自動では閉じません。

## 変更しないもの

- IndexedDB、バックアップ、クラウド同期、Supabaseスキーマ、既存メモのデータ構造は変更していません。
- Supabase SQLの追加実行は不要です。
