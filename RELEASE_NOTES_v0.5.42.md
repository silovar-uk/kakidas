# kakidas v0.5.42 — 項目ごとのコピー

## 変更

- 単語・文・段落の各項目で、`…` の操作メニューから **コピー** を選べるようにした
- コピー対象は、その項目の本文のみ。段落の改行はそのまま保持する
- PCでは項目下の操作メニュー、スマホではボトムシート内に表示する
- コピー成功時は既存と同じ一時通知を表示し、5秒後または `×` で閉じられる

## 影響範囲

- IndexedDB・バックアップ・クラウド同期・Supabase SQLの変更はない
- 単語／文／段落の一覧コピーの仕様は変更しない
