# kakidas v0.5.44 — 単一タグ・一覧整理・タグ管理

## 追加

- 各メモに、自由入力のタグを **0または1つ** 付けられるようにしました。
- 編集画面のタイトル付近に `＋ タグ` を追加しました。
  - Enterまたは保存ボタンで確定
  - 過去に使ったタグを最大6件、入力内容の前方一致で候補表示
  - 候補を選ぶと、そのタグをそのまま再利用
  - `×` でタグを外せる
- メモ一覧にタグのチップを表示しました。
- メモ一覧で、`更新が新しい順`／`作成が新しい順` を選べるようにしました。
  - 並び順は端末ごとの表示設定で、メモの日時データは変更しません。
- メモ一覧で、タグによる絞り込みを追加しました。
- `/tags` の **タグ管理** 画面を追加しました。
  - 現在使われているタグと件数を一覧表示
  - タグ名をまとめて変更
  - タグを付けている全メモからまとめて解除
  - タグ単体のマスタは作らず、候補は常に実際にメモに付いているものだけ

## データ・同期

- `memos.tag` を追加しました。タグはメモの属性であり、単語・文・段落には付きません。
- 既存のIndexedDB・v1〜v5バックアップ・古いクラウドメモは、タグなし（`null`）として安全に読み込みます。
- バックアップ形式をv6へ更新しました。過去のバックアップはそのまま取り込めます。
- タグ名変更・解除は、対象メモをひとつのIndexedDBトランザクションで更新します。
- クラウド連携を使う既存プロジェクトでは、`supabase/MIGRATE_v0.5.44.sql` をSQL Editorで一度実行してください。未実行のままタグ付きメモを送ると、クラウド送信は失敗します。
