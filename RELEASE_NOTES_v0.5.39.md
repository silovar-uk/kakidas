# kakidas v0.5.39 — 並び替え操作の整理

## 変更

- Word / Sentenceの操作メニューから「右へ下げる」「左へ戻す」を削除
  - Tab、Shift + Tab、修飾キー + 左右矢印による階層移動も廃止
  - 既存の親子構造はそのまま表示・保存され、`＋ 下に追加` で子項目を追加できる
- 同じ階層・同じ完了状態の中で、`⇡ 一番上に移動` と `⇣ 一番下に移動` を追加
- `↑ 上へ移動` / `↓ 下へ移動` は、操作メニューを閉じずに連続実行できるよう変更
  - スマホの操作シートも、上・下移動の間は開いたまま
- 「文へ移動」は青系、「段落へ移動」は紫系へ色分けし、移動先をひと目で判別しやすく変更

## データ・同期

- 親子関係、既存メモ、バックアップ、クラウド同期のデータ形式は変更なし
- Supabase SQLの追加実行は不要
