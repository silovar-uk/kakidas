# kakidas v0.5.14

## 完了とメモ一覧コピー

- 単語・文・段落の各項目に、小さな「完了」ボタンを追加
- 完了すると項目はグレーアウトし、同じ階層内の末尾へ移動
- もう一度タップすると「戻す」として未完了へ復帰
- 項目単位のコピー機能を削除
- メモ一覧の各メモに「コピー」を追加
- メモ一覧からのコピーは、完了済み項目を含めない
- 番号表示がONなら、メモ一覧からのコピーにも番号を反映
- 備考欄を「気持ち・備考」として表示し、書いたときの感情や補足を残せるよう整理
- 完了状態はIndexedDB・バックアップ・クラウド送信・取り込み・複製取り込みで保持

## Supabaseを使う場合

既存のSupabaseプロジェクトでは、GitHub / Vercelへ反映した後に `supabase/MIGRATE_v0.5.14.sql` をSQL Editorで一度実行する。
