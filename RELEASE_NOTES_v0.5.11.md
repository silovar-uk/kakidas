# kakidas v0.5.11 — 各項目の任意の備考

## 変更

- 単語・文・段落の各項目に、任意の「備考」を追加できるようにした。
- 備考が空の項目では、備考の行・余白を一切表示しない。
- 項目右側の「＋備考」から補足を書ける。備考がある項目では、本文の下にだけ小さく表示される。
- 備考は編集・削除でき、本文の保存と同じくローカル保存・クラウド送信・クラウド取り込み・複製取り込みの対象になる。
- 個別コピー、区分コピー、すべてコピー、`.txt`出力では、備考がある場合だけ `備考：...` として含める。
- 既存IndexedDBの項目には空文字の備考を補完するため、既存メモの見た目は変わらない。

## Supabaseを使っている場合

既存のSupabaseプロジェクトでは、`supabase/MIGRATE_v0.5.11.sql` をSQL Editorで一度実行する。

```sql
alter table public.entries
  add column if not exists note text not null default '';
```
