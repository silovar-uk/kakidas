# kakidas リリースノート

v0.5.5以降の変更履歴を、この1ファイルに統合している。過去のバージョン別ノートは内容を省略せず、見出し単位で保存した。

- 直近の変更は末尾から確認する。
- 次回以降は、新しい `## kakidas v0.5.xx` 節をこのファイル末尾へ追記する。
- Supabase移行の有無は、各バージョンの節と `supabase/` をあわせて確認する。

---

### kakidas v0.5.5 — Mobile composer normal-flow fix

### Fixed

On mobile widths, the entry composer no longer uses `position: fixed`.

The previous fixed composer could grow (especially with Paragraph input), cover most of the viewport, and intercept touches meant for the memo list, tabs, copy, and delete controls.

### New behavior

- The composer remains in the normal document flow under the active section guide.
- The memo list and all surrounding controls remain tappable.
- Long Paragraph fields are constrained to an internal scroll area rather than expanding across the viewport.
- A final mobile-only CSS safeguard prevents future rules from turning the composer into a fixed overlay.

### Deployment

No Supabase, Google Cloud, or Vercel environment-variable changes are required.
Replace the project files in GitHub and redeploy through Vercel.

---

### kakidas v0.5.6 — Entry created timestamps

### Added

- Every Word, Sentence, and Paragraph now displays its original creation timestamp.
- The timestamp remains visible while editing the item.
- Time is shown in the device's local timezone as `YYYY/MM/DD HH:MM`.

### Cloud import

- When a cloud memo is imported as a copy, entry `created_at` values are preserved instead of being reset to the import time.
- Existing hierarchy, sort order, and kind handling stay unchanged.

### Deployment

No Supabase schema, Google Cloud, or Vercel environment-variable changes are required.

---

### kakidas v0.5.7 — Entry timestamp visibility

### Added

- Added an `項目の日時を表示` switch in the memo editor.
- The switch controls whether created-at timestamps appear for all Word / Sentence / Paragraph entries.
- The default is on, preserving v0.5.6 behavior.

### Behavior

- The choice is stored only in the current browser with localStorage.
- It does not alter entry data, cloud uploads, or imported timestamps.
- When hidden, timestamps are removed from both the normal item view and the edit view.

### Deployment

No Supabase schema, Google Cloud, or Vercel environment-variable changes are required.

---

### kakidas v0.5.8

- 表示名を Word / Sentence / Paragraph から 単語 / 文 / 段落 へ変更
- 入力欄の補助文を簡潔化
- PCカラム見出しとスマホタブを静かな小型UIへ変更
- 項目カードの種別表記を出さず本文中心に整理
- 階層の見た目をインデントと細いガイド線へ統一
- 操作文を「下に追加」「右へ下げる」「左へ戻す」などに統一
- DBの kind / URL / クラウド同期仕様は変更なし

---

### kakidas v0.5.9 — 振り番の表示・出力

### 追加

- メモ編集画面に「番号を表示」スイッチを追加。初期状態はオフ。
- 単語・文・段落の全項目に、現在の並び順に基づく振り番を表示できる。
- 階層化した単語・文は `1` / `1.1` / `1.1.1` のように、階層を反映して番号を再計算する。
- 番号を表示中は、全体コピー・種別ごとのコピー・.txt出力・個別コピーにも同じ番号を含める。
- 並び替え、右へ下げる、左へ戻す後も、番号は保存せずその場で再計算する。

### 変更しないもの

- entries テーブル、kind、parent_id、sort_order、クラウド同期の構造は変更なし。
- 番号は画面表示・コピー用の派生情報で、IndexedDBやSupabaseへ新しい列は追加しない。
- 番号表示の設定は端末・ブラウザごとのlocalStorageに保存する。

---

### kakidas v0.5.10 — 項目日時の年表示を省略

### 変更

- Word / Sentence / Paragraphの各項目に表示する作成日時を、`YYYY/MM/DD HH:MM` から `MM/DD HH:MM` に変更。
- 年を含む元の `created_at` はIndexedDB・Supabase・クラウド取り込み時のデータとしてそのまま保持。
- 日時を表示する設定、コピー、階層、番号表示、クラウド同期の動作は変更なし。

---

### kakidas v0.5.11 — 各項目の任意の備考

### 変更

- 単語・文・段落の各項目に、任意の「備考」を追加できるようにした。
- 備考が空の項目では、備考の行・余白を一切表示しない。
- 項目右側の「＋備考」から補足を書ける。備考がある項目では、本文の下にだけ小さく表示される。
- 備考は編集・削除でき、本文の保存と同じくローカル保存・クラウド送信・クラウド取り込み・複製取り込みの対象になる。
- 個別コピー、区分コピー、すべてコピー、`.txt`出力では、備考がある場合だけ `備考：...` として含める。
- 既存IndexedDBの項目には空文字の備考を補完するため、既存メモの見た目は変わらない。

### Supabaseを使っている場合

既存のSupabaseプロジェクトでは、`supabase/MIGRATE_v0.5.11.sql` をSQL Editorで一度実行する。

```sql
alter table public.entries
  add column if not exists note text not null default '';
```

---

### kakidas v0.5.12

### デフォルトタイトル

- 新規メモのタイトルを `m/d hh:mm 「」` 形式へ変更
  - 例：`6/23 08:12 「」`
- 年を含む日時は `created_at` にこれまでどおり保持
- 空のかぎ括弧は、あとからテーマや補足を書き足すための余白
- 既存メモのタイトル、クラウド同期、DB構造は変更なし

---

### kakidas v0.5.13

### 各項目の満足度

- 単語・文・段落の各項目に、0〜5の満足度を追加
- 数字の小さなボタンをタップすると、`0 → 1 → 2 → 3 → 4 → 5 → 0` と循環
- 低い値は落ち着いた青、高い値は穏やかな赤へ変化
- 画面幅を取らないよう、ラベルなしの小さな数値ボタンとして表示
- 満足度はローカル保存、バックアップ、クラウド送信、取り込み、複製取り込みで保持
- コピーや`.txt`出力には含めず、本文を静かに保つ

### Supabaseを使う場合

既存のSupabaseプロジェクトでは、`supabase/MIGRATE_v0.5.13.sql` をSQL Editorで一度実行する。

---

### kakidas v0.5.14

### 完了とメモ一覧コピー

- 単語・文・段落の各項目に、小さな「完了」ボタンを追加
- 完了すると項目はグレーアウトし、同じ階層内の末尾へ移動
- もう一度タップすると「戻す」として未完了へ復帰
- 項目単位のコピー機能を削除
- メモ一覧の各メモに「コピー」を追加
- メモ一覧からのコピーは、完了済み項目を含めない
- 番号表示がONなら、メモ一覧からのコピーにも番号を反映
- 備考欄を「気持ち・備考」として表示し、書いたときの感情や補足を残せるよう整理
- 完了状態はIndexedDB・バックアップ・クラウド送信・取り込み・複製取り込みで保持

### Supabaseを使う場合

既存のSupabaseプロジェクトでは、GitHub / Vercelへ反映した後に `supabase/MIGRATE_v0.5.14.sql` をSQL Editorで一度実行する。

---

### kakidas v0.5.15

### 完了済みの整理

- 編集画面に「完了を非表示」スイッチを追加しました。初期状態は表示です。
- 「完了を削除（件数）」で、単語・文・段落をまたいだ完了済み項目をまとめて削除できます。
- 削除確認では、未完了の項目を残すことを明示します。
- 完了済みの親の下に未完了項目がある場合、未完了項目を削除せず、近い残る階層へ移します。

### データと同期

- 既存の `is_completed` カラムを利用するため、Supabaseの追加SQLは不要です。
- 完了を非表示にする設定は端末・ブラウザごとの表示設定で、クラウドには保存しません。
- 完了済みの削除はソフト削除としてクラウド送信・バックアップに反映されます。

---

### kakidas v0.5.16

### モバイルの見やすさ調整

- 完了済み項目のグレーアウトを濃くし、本文・備考・日時もより明確に淡く表示します。
- 満足度0〜5の数字ボタンは、落ち着いた青〜赤の色味を保ったまま、2pxの色枠で判別しやすくしました。
- スマホでは、文・段落の入力欄の右にある「置く」ボタンを52px固定にし、入力欄を優先して残り幅へ広げます。
- スマホの各項目右側の操作は2列の小さな縦スタックに整理し、本文が画面幅の概ね4分の3以上を使えるようにしました。
- 気持ち・備考の操作はスマホでは小さな「＋」または「✎」で表示し、幅を取りません。

### データと同期

- DB構造、Supabase、クラウド送信・取り込み、既存データは変更ありません。
- Supabase SQLの追加実行は不要です。

---

### kakidas v0.5.17

### 追加

- 単語・文・段落の各見出しに、区分ごとの `コピー` を追加しました。
  - 完了済みは含めません。
  - 振り番表示が有効な場合は、コピー内容にも反映されます。
- メモ一覧の `コピー` を、スマホでも失敗しにくい方式へ改善しました。
  - 一覧表示中に内容を先読みし、タップ中にClipboard APIを呼べるようにしています。
  - `The request is not allowed` でNative Clipboard APIが拒否された場合は、選択コピーへ自動でフォールバックします。

### 変更なし

- Supabaseのテーブル、RLS、クラウド同期のデータ構造は変更していません。
- 新しいSQLやVercelの環境変数追加は不要です。

---

### kakidas v0.5.18 — 完了済みをコピーに含める選択

### 追加

- メモ編集画面とメモ一覧に、`コピーに完了を含める` スイッチを追加。
- 初期状態はオフ。これまでどおり、コピーには未完了の項目だけを含める。
- オンにすると、単語／文／段落の区分コピーと、メモ一覧からのメモ全体コピーの両方に完了済み項目も含める。
- 選択は端末・ブラウザごとに保存され、一覧と編集画面で共通。
- `.txt`出力はこれまでどおり、完了済みを含む記録用の出力として維持。

### 変更しないもの

- DB・Supabase・クラウド同期・バックアップの構造
- 完了状態、階層、振り番、備考、満足度の保存方法

---

### kakidas v0.5.19 — Quiet writing surface

### 変更

- PCの単語・文・段落を、カードではなく細い列間罫線で分ける構成へ変更
- 各項目を本文中心の行表示へ変更し、強い背景・角丸・枠線の重なりを削減
- 完了済み項目を区分の下部へまとめ、「完了 n件」から開閉可能に変更
- 表示設定・コピー設定・完了一括削除を「表示・整理」内へ格納
- 区分コピーは見出しの小アイコン、区分一括削除は見出しの `⋯` 内へ移動
- 気持ち・備考は常設のまま、モバイルで本文下へ移動して右側の操作幅を抑制
- モバイルの `⋯` 操作シートを段落にも対応させ、削除を右側常設から移動

### 変更しないもの

- 満足度の0〜5表示と色・枠線の設計
- 備考を常設する方針
- IndexedDB、Supabase、クラウド同期、URL、既存データ構造

### Supabase

SQLの追加実行は不要です。

---

### kakidas v0.5.20 — クラウド削除とモバイル備考＋

### 変更

- クラウドのメモ一覧から、各メモを**クラウドだけから削除**できるように追加
  - 削除前に確認を表示
  - この端末のローカルメモ・項目は残る
  - 削除後、同じローカルメモは「ローカルのみ」状態へ戻り、必要なら改めて送信できる
  - クラウド側の関連項目はメモと同時に削除される
- スマホでは、本文下の `＋ 気持ち・備考` を非表示に変更
- スマホの右側操作レールへ、コンパクトな `＋` ボタンを追加
  - `＋` から備考を追加・編集できる
  - 備考本文がある場合は、これまでどおり本文下にだけ表示
- PCでは、従来どおり本文下の `＋ 気持ち・備考` を常設

### Supabase

既存の `supabase/schema.sql` にある `memos` / `entries` のDELETEポリシーと
`ON DELETE CASCADE` を利用します。追加のSQL実行は不要です。

---

### kakidas v0.5.21

### 修正

- スマホの備考ボタンで `＋` が二重に表示される問題を修正しました。
- 右側の備考ボタンは、常に記号を1つだけ描画します。
- 備考がある項目では、従来どおり編集を示す記号が表示されます。

### 変更なし

- ローカル保存・クラウド同期・Supabaseスキーマは変更ありません。

---

### v0.5.22 — 新しい項目の追加位置

- 単語・文・段落の新しい項目は、初期状態で同じ階層の一番上に追加されるように変更。
- `表示・整理` に `新しい項目を一番下に追加` の切り替えを追加。オンにすると、同じ階層の末尾へ追加する。
- この設定は端末・ブラウザごとの表示設定として保存し、クラウド・バックアップ・DB構造には保存しない。
- 親の下へ追加する場合も、設定に応じてその階層の先頭／末尾へ追加する。
- `sort_order` を安全に計算するため、既存の親子構造・並び順・番号表示・クラウド同期は維持。
- Supabase SQLの追加実行は不要。

---

### kakidas v0.5.23

### 項目を長い粒度へ移動

- 単語を「文」または「段落」へ移動できるようにしました。
- 文を「段落」へ移動できるようにしました。
- 移動先では、項目は先頭のルート項目として表示されます。
- 下に項目がある場合は確認を表示します。移動するのは選んだ項目だけで、下の項目は元の区分に残ります。
- kind、並び順、同期対象の情報は既存の仕組みを使い、Supabaseの追加SQLは不要です。

### 操作場所

- PC: 項目右側の `⋯` を開き、「文へ移動」「段落へ移動」を選びます。
- スマホ: 項目右側の `⋯` を開き、「移動先」から選びます。

---

### v0.5.24 — PC備考ボタンの簡素化

- PCでも備考の操作ボタンを、常時表示の「＋」だけに変更しました。
- 「＋ 気持ち・備考」「気持ち・備考」の長い表示をなくし、本文と備考本文を主役にしています。
- 既存の備考内容、モバイル側の右レール操作、クラウド同期、データ構造は変更していません。

---

### v0.5.25 — 表示・整理ボタンの視認性調整

- 「表示・整理」を、淡い暖色の背景・細い枠線・角丸を持つボタンへ変更しました。
- 開いている間は、背景と枠線が少し濃くなり、状態が分かりやすくなります。
- 他の機能、同期、データ構造には変更はありません。

---

### v0.5.26｜本文だけ表示

- 「表示・整理」に `本文だけ表示` を追加
- 単語・文・段落の本文だけを、密な一覧として表示
- 備考、日時、満足度、完了、追加・削除・階層操作、入力欄を表示から隠す
- 親子階層のインデントとガイド線、完了済みのグレー表示は維持
- 段落は2行までの省略表示で、一覧性を優先
- 表示設定は端末・ブラウザごとに保存。DB・クラウド同期・コピー内容は変更なし

---

### v0.5.27｜項目の並び順を選択

- `表示・整理` に **並び順** を追加
  - 追加順（昇順）
  - 追加順（降順）
  - 評価順（高い順）
- 単語・文・段落の各区分で、同じ設定を適用
- 単語・文の階層は保ったまま、同じ階層内の項目だけを選択順に並べ替え
- 完了済みは従来どおり各階層の末尾・完了エリア側に残る
- 番号表示をオンにしている場合は、選んだ順序に合わせて `1 / 1.1` の番号も再計算
- 区分コピー、メモ全体コピー、`.txt`出力にも選択中の並び順を反映
- 並び順は端末・ブラウザごとの表示設定。DB、クラウド同期、既存データは変更しない

---

### v0.5.28｜操作の置き場所を整理

- `クラウドへ送る` と `.txt出力` を `表示・整理` の中へ移動
- `並び順` をメモ削除ボタンの左へ移動し、表示中でもすぐ変更できるように調整
- スマホでも並び順と削除が同じ行に収まり、出力・クラウド操作は `表示・整理` を開いた時だけ表示
- DB、Supabase、クラウド同期、既存データへの変更はなし

---

### v0.5.29｜スマホタブの未完了件数

- スマホの「単語／文／段落」タブに、未完了項目数を小さく表示
- 完了済み項目はタブの件数に含めない
- 件数は追加・完了・削除・移動のたびに自動更新
- 表示設定、DB、クラウド同期、既存データへの変更はなし

---

### v0.5.30｜ブラウザタブにメモタイトルを表示

- メモ編集画面を開くと、Chromeなどのブラウザタブにメモのタイトルを表示
- タイトルを編集中も、入力内容をすぐタブ名へ反映
- メモ一覧へ戻ると、タブ名は `kakidas` に戻る
- DB・クラウド同期・既存データの変更はなし

---

### kakidas v0.5.31 — 項目ごとのリンク

### 追加

- 単語・文・段落の各項目に、任意の外部リンクを1件登録できるようにしました。
- URLそのものは通常の一覧では表示せず、短い鎖アイコンだけで扱います。
  - リンク未登録：鎖アイコンを押すとURL入力を開く
  - リンク登録済み：鎖アイコンを押すと新しいタブで開く
- 既存リンクの編集・解除は、項目本文を編集したときに表示されるURL欄から行えます。
- `example.com` のようにプロトコルなしで貼り付けた場合は、保存時に `https://` を補います。
- コピーと `.txt` 出力には、登録済みリンクを `リンク：` として含めます。

### モバイル

- 右側の操作レールを広げず、本文下に小さな鎖アイコンを1つだけ置きます。
- URL入力は編集時だけ開き、通常の一覧に長いURL文字列は表示しません。

### データ・クラウド

- ローカルのIndexedDBはv8へ自動移行し、既存の項目に空の `link_url` を補完します。
- Supabaseを使っている既存プロジェクトでは、`supabase/MIGRATE_v0.5.31.sql` をSQL Editorで一度実行してください。
- クラウド送信・取り込み・バックアップ・コピー・`.txt`出力でリンクを保持します。

---

### kakidas v0.5.32 — リンク状態の見分けやすさ

### 変更

- 項目ごとの鎖アイコンで、リンクの未登録・登録済みを色で見分けられるようにしました。
  - 未登録：控えめなグレー
  - 登録済み：淡い青緑の背景と青緑のアイコン
- アイコンの大きさ・置き場所は変えず、スマホでも短い表示のままです。

### データ・クラウド

- URLデータ、コピー、`.txt`出力、クラウド同期の仕様は変更していません。
- Supabase SQLの追加実行は不要です。

---

### kakidas v0.5.33 — iPhone Safari安定化

### 修正

- iPhone Safariで「新しいメモ」が作成中のままになったり、既存メモが「読み込んでいます」で止まる場合がある問題を対策。
- IndexedDBアップグレード時に複数のcursorを同時に実行していた処理を廃止。
  - 旧レコードの `parent_id` / `note` / `satisfaction` / `is_completed` / `link_url` は、従来どおり読み込み時に安全に補完する。
  - これによりデータを失わず、Safariの保存領域アップグレードが止まりにくくなる。
- IndexedDBが別タブなどでブロックされた場合や、12秒間開けない場合は、無限に「読み込み中」のままにせず理由を表示。
- 一覧・編集画面に「もう一度読み込む」を追加。
- Safariで画面を離れる時にDB接続を閉じ、古い画面が新しいバージョンの保存領域更新を妨げにくくした。

### 影響

- 既存メモ、備考、リンク、満足度、完了状態、クラウド同期データはそのまま。
- Supabase SQLの追加実行は不要。

---

### kakidas v0.5.34 — 複数タブでの保存領域安定化

### 修正

- iPhone Safariを含め、複数のkakidasタブを開いた状態でも、保存領域のバージョン更新を互いに妨げにくくしました。
- タブが背景へ回った時は、画面を閉じずにIndexedDB接続だけを解放します。
  - 前面に戻った後は、次の読み書き時に自動で接続し直します。
- IndexedDBの更新要求が別タブの接続解放を待つ場合、すぐに「ほかのタブを閉じて」と失敗扱いにせず、接続を譲る時間を待ちます。
- 別タブから保存領域の更新を求められた接続は、自動で閉じて次の接続を妨げないようにしました。

### 影響

- メモ本文、階層、作成日時、備考、リンク、満足度、完了状態、クラウド同期、バックアップのデータ構造は変更ありません。
- Supabase SQLの追加実行は不要です。
- 旧版を開きっぱなしにしている初回だけは、旧タブの再読み込みまたは終了が必要になる場合があります。v0.5.34へ更新後は、複数タブを開いたまま使う前提です。

---

### kakidas v0.5.35 — モバイルのメモ削除確認

### 修正

- メモ一覧の `×` による削除確認を、ブラウザ標準の `window.confirm()` からアプリ内の確認画面へ変更しました。
- iPhone Safariで標準確認が表示・復帰しない場合でも、`×` を押すと削除確認を開き、そこで `削除する` を選ぶとローカルメモを削除できます。
- `×` の大きさ・置き場所・メモ一覧のレイアウトは変更していません。
- 削除は従来どおりローカルのメモと項目にだけ反映され、クラウドへ送ったコピーは残ります。

### データ・同期

- IndexedDBの構造、Supabase、クラウド同期、既存メモ、バックアップ形式に変更はありません。
- Supabase SQLの追加実行は不要です。

---

### kakidas v0.5.36 — メモ削除・アイコン・タブ名の統合修正

### 修正

- メモ一覧の `×` は、サイズ・置き場所・一覧レイアウトを変えずに、削除確認をアプリ内ダイアログへ切り替えました。
  - iPhone Safariのブラウザ標準 `window.confirm()` に依存しません。
  - `×` のクリックはカードを開く操作から明示的に切り離し、削除確認を確実に開きます。
  - 削除確定後は、ローカルのメモと項目を一覧から削除します。
- 実際に使っているアイコン一式を `public/` に復旧しました。
  - favicon / Apple Touch Icon / Android Chrome icon / Windows tile を元のファイル名で配置しています。
  - アイコン・manifestのURLへバージョン識別子を付け、更新後に古いキャッシュを引きずりにくくしました。
- メモ編集画面を開いた時のブラウザタブ名をメモ名にする挙動を、現行ソースとして統合しました。
  - 編集中のタイトル入力もタブ名へ反映します。
  - メモ一覧へ戻ると、タブ名は `kakidas` に戻ります。

### 継続している安全策

- v0.5.34の複数タブ対策を維持しています。
  - 背景タブではIndexedDB接続だけを解放します。
  - 別タブの接続解放待ちを即エラーにしません。
  - versionchange時は古いDB接続を自動で閉じます。

### データ・同期

- IndexedDBの構造、Supabase SQL、クラウド同期、既存メモ、バックアップ形式に変更はありません。
- Supabase SQLの追加実行は不要です。

---

### kakidas v0.5.37 — 一時通知の自動消去

### 変更

- 「完了済み3件を削除しました。」「コピーしました。」「バックアップを書き出しました。」などの完了通知を、表示から5秒後に自動で閉じるようにしました。
- 通知の右側に `×` を追加し、待たずに手動で閉じられるようにしました。
- メモ一覧、メモ編集画面、クラウドライブラリで同じ挙動に統一しました。
- エラー表示や削除確認ダイアログは、自動では閉じません。

### 変更しないもの

- IndexedDB、バックアップ、クラウド同期、Supabaseスキーマ、既存メモのデータ構造は変更していません。
- Supabase SQLの追加実行は不要です。

---

### kakidas v0.5.38 — 段落入力の改行優先

### 変更

- 「段落」は、Enterだけでは置かず、改行として入力されるように変更しました。
- 段落を置く操作は、`Shift＋Enter` または `Ctrl＋Enter`、または画面の `置く` ボタンです。
- 単語・文は、これまでどおりEnterで置けます。
- 段落の入力欄に、ショートカットの案内を表示します。

### 変更しないもの

- IndexedDB、バックアップ、クラウド同期、Supabaseスキーマ、既存メモのデータ構造は変更していません。
- Supabase SQLの追加実行は不要です。

---

### kakidas v0.5.39 — 並び替え操作の整理

### 変更

- Word / Sentenceの操作メニューから「右へ下げる」「左へ戻す」を削除
  - Tab、Shift + Tab、修飾キー + 左右矢印による階層移動も廃止
  - 既存の親子構造はそのまま表示・保存され、`＋ 下に追加` で子項目を追加できる
- 同じ階層・同じ完了状態の中で、`⇡ 一番上に移動` と `⇣ 一番下に移動` を追加
- `↑ 上へ移動` / `↓ 下へ移動` は、操作メニューを閉じずに連続実行できるよう変更
  - スマホの操作シートも、上・下移動の間は開いたまま
- 「文へ移動」は青系、「段落へ移動」は紫系へ色分けし、移動先をひと目で判別しやすく変更

### データ・同期

- 親子関係、既存メモ、バックアップ、クラウド同期のデータ形式は変更なし
- Supabase SQLの追加実行は不要

---

### kakidas v0.5.40 — メモ一覧の内容プレビュー

### 変更

- メモ一覧の各カードに、入力済みの **単語・文・段落** から最大1件ずつの抜粋を淡く表示
- 一覧を開いた時点でコピー用に先読みしているメモ詳細を利用するため、IndexedDBやSupabaseのデータ構造は変更なし
- 未完了の項目を優先して表示し、未完了がないメモでは完了済みの項目を代わりに表示
- 改行を含む段落は一覧では1行へ整形し、長い内容は省略記号付きで短く表示
- モバイルでは抜粋の種別ラベルを隠し、カードの幅を圧迫しない

### 影響範囲

- IndexedDB / バックアップ / Supabase / SQL の変更はなし
- 既存メモの内容、並び順、完了状態、クラウド同期仕様は変更なし

### 確認

- `npm run build`
- PC / iPhone Safariで、一覧のタイトル・操作ボタン・内容抜粋が重ならないこと

---

### kakidas v0.5.41 — 初期タイトルと区分移動の往復

### 変更

- 新しく作るメモの初期タイトルを、`M/D` のみへ変更
  - 例：`6/27`
  - 作成日時そのものは、これまでどおり `created_at` に保持
  - 時刻と空のかぎ括弧は初期タイトルから外した
- 区分移動に、短い粒度へ戻す選択肢を追加
  - 文 → 単語
  - 段落 → 文
- 既存の 単語 → 文 / 段落、文 → 段落 は継続
- 単語へ移動するボタンも、文・段落への移動と同様に色で判別できるように追加

### 影響範囲

- 移動は内容を分割せず、移動先区分のルート項目として置く
- 移動元に子項目がある場合、既存仕様どおり子は元の区分内で繰り上げて残す
- IndexedDB / バックアップ / Supabase / SQL の変更はなし
- 既存メモのタイトル・作成日時・内容は変更しない

### 確認

- 新規メモが `M/D` で作られること
- 文 → 単語、段落 → 文がPC・スマホの操作メニューから実行できること
- 移動後も項目内容、備考、リンク、満足度、完了状態が保持されること
- `npm run build`

---

### kakidas v0.5.42 — 項目ごとのコピー

### 変更

- 単語・文・段落の各項目で、`…` の操作メニューから **コピー** を選べるようにした
- コピー対象は、その項目の本文のみ。段落の改行はそのまま保持する
- PCでは項目下の操作メニュー、スマホではボトムシート内に表示する
- コピー成功時は既存と同じ一時通知を表示し、5秒後または `×` で閉じられる

### 影響範囲

- IndexedDB・バックアップ・クラウド同期・Supabase SQLの変更はない
- 単語／文／段落の一覧コピーの仕様は変更しない

---

### kakidas v0.5.43 — 項目から新しいメモへ展開

### 追加

- 単語・文・段落の `…` メニューに `新しいメモにする` を追加しました。
- 元の項目は残したまま、新しいメモの最初の項目として複製します。
- 新しいメモのタイトルは `M/D｜本文冒頭` で始まります。段落の改行はタイトル内で空白へ整え、長い本文は末尾を `…` で省略します。
- 本文、備考、リンク、満足度は引き継ぎます。完了状態は未完了へ戻し、親子関係は持ち込まずルート項目として作成します。
- 作成後は新しいメモへ移動し、元の項目と同じ区分の入力欄を開きます。

### データ・同期

- 新しいメモと最初の項目は、ひとつのIndexedDBトランザクションで作成します。途中で失敗した場合に片方だけ残りません。
- 新メモの同期状態は `local_only`。クラウド送信は既存どおりユーザーが明示して行います。
- IndexedDBのスキーマ、バックアップ形式、Supabase schema、移行SQLの変更はありません。

---

### kakidas v0.5.44 — 単一タグ・一覧整理・タグ管理

### 追加

- 各メモに、自由入力のタグを **0または1つ** 付けられるようにしました。
- 編集画面のタイトル付近に `＋ タグ` を追加しました。
  - Enterまたは保存ボタンで確定
  - 過去に使ったタグを最大6件、入力内容の前方一致で候補表示
  - 候補を選ぶと、そのタグをそのまま再利用
  - `×` でタグを外せる
- メモ一覧にタグのチップを表示しました。
- メモ一覧で、`更新が新しい順`／`作成が新しい順` を選べるようにしました。
  - 並び順は端末ごとの表示設定で、メモの日時データは変更しません。
- メモ一覧で、タグによる絞り込みを追加しました。
- `/tags` の **タグ管理** 画面を追加しました。
  - 現在使われているタグと件数を一覧表示
  - タグ名をまとめて変更
  - タグを付けている全メモからまとめて解除
  - タグ単体のマスタは作らず、候補は常に実際にメモに付いているものだけ

### データ・同期

- `memos.tag` を追加しました。タグはメモの属性であり、単語・文・段落には付きません。
- 既存のIndexedDB・v1〜v5バックアップ・古いクラウドメモは、タグなし（`null`）として安全に読み込みます。
- バックアップ形式をv6へ更新しました。過去のバックアップはそのまま取り込めます。
- タグ名変更・解除は、対象メモをひとつのIndexedDBトランザクションで更新します。
- クラウド連携を使う既存プロジェクトでは、`supabase/MIGRATE_v0.5.44.sql` をSQL Editorで一度実行してください。未実行のままタグ付きメモを送ると、クラウド送信は失敗します。

---

### kakidas v0.5.45｜段落入力時のモバイル安定化

- 段落入力欄の自動高さ調整を見直し、入力中は必要な時だけ伸ばす方式へ変更
- 文字を消した時の縮小は、`置く` の後または入力欄から離れた時だけに限定
- 日本語IMEの変換中は高さ測定を保留し、変換確定後に一度だけ調整
- 段落が最大高を超えた場合は、画面全体ではなく入力欄の中でスクロール
- iPhone Safariで、1文字ごとの入力に伴ってページ位置が少しずつ上がる問題の抑制を目的とする
- IndexedDB・バックアップ・クラウド同期・Supabase SQLの変更なし

---

## kakidas v0.5.46｜配布構成の整理

- `RELEASE_NOTES_v0.5.5.md`〜`RELEASE_NOTES_v0.5.45.md` を `RELEASE_NOTES.md` へ統合
- `node_modules`、`dist`、`.git` を除く配布用ソースのファイル数を108件から68件へ整理
- アプリの機能、IndexedDB、バックアップ、クラウド同期、Supabase SQLに変更なし
- 今後の変更履歴は、バージョンごとの新規ファイルではなくこのファイルの末尾へ追記する

---

## kakidas v0.5.47｜満足度に応じた項目背景

- 各単語・文・段落の背景を、満足度0〜5に応じてごく淡く色分け
  - 低い値は青〜緑寄り、高い値は暖色寄り
  - 背景だけを静かに変え、本文・操作ボタンの視認性は保つ
- ホバー、編集中、操作メニューを開いた時も、同じ満足度の色味を保ったまま少しだけ強調
- 本文だけ表示でも淡く反映
- 完了済み項目は従来の灰色表示を優先し、満足度色とは混ぜない
- IndexedDB・バックアップ・クラウド同期・Supabase SQLの変更なし
---

## kakidas v0.5.48｜空の初期タイトル

- 空の新規メモの既定タイトルを、`M/D` から `M/D「」` へ変更
- 項目から新しいメモを作る場合は、従来どおり `M/D｜本文冒頭` を使用
- 既存メモのタイトルは自動で変更しない
- IndexedDB・バックアップ・クラウド同期・Supabase SQLの変更なし

---

## kakidas v0.5.49｜PCの項目補助操作を右側へ

- PCでは、項目の備考 `＋` とリンクを、完了ボタンの右側にまとめました。
- 本文下に出ていた補助操作を右側へ寄せ、項目カードをできるだけ一行に収めます。
- スマホは従来どおり、本文下のリンクと右側の `＋` を維持します。
- IndexedDB・バックアップ・クラウド同期・Supabase SQLの変更はありません。

---

## kakidas v0.5.50｜満足度0は通常背景

- 満足度0の単語・文・段落は、通常の項目カードと同じ背景色に変更
- ホバー、`…` の操作メニュー表示時、本文だけ表示でも、満足度0には色味を付けない
- 満足度1〜5の淡い背景色と、完了済み項目の灰色表示は従来どおり
- IndexedDB・バックアップ・クラウド同期・Supabase SQLの変更なし

## kakidas v0.5.51｜番号と「気持ち・備考」の主従

- 番号表示時、本文・気持ち・備考・作成日時を番号の右側に揃えるレイアウトへ変更。
- 「気持ち」が番号の実文字より左に出ず、番号 → 本文 → 補助情報の順が読み取れるようにした。
- 読み取り時だけでなく、気持ち・備考の編集時も同じ基準で揃える。
- データ、バックアップ、クラウド同期、Supabase SQLの変更なし。
