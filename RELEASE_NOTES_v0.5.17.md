# kakidas v0.5.17

## 追加

- 単語・文・段落の各見出しに、区分ごとの `コピー` を追加しました。
  - 完了済みは含めません。
  - 振り番表示が有効な場合は、コピー内容にも反映されます。
- メモ一覧の `コピー` を、スマホでも失敗しにくい方式へ改善しました。
  - 一覧表示中に内容を先読みし、タップ中にClipboard APIを呼べるようにしています。
  - `The request is not allowed` でNative Clipboard APIが拒否された場合は、選択コピーへ自動でフォールバックします。

## 変更なし

- Supabaseのテーブル、RLS、クラウド同期のデータ構造は変更していません。
- 新しいSQLやVercelの環境変数追加は不要です。
